<div class="container mx-auto p-4">

 <div class="flex justify-between items-center fixed top-0 left-0 right-0 bg-white z-50 p-4 shadow">
  <h2 class="text-xl font-semibold">Bonjour, {{ userName }}</h2>
  <button (click)="logout()" class="bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded">
    Déconnexion
  </button>
</div>


  <h1 class="text-2xl font-bold mb-4">Produits disponibles</h1>

  <!-- Liste produits -->
  <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
    <div *ngFor="let product of products" class="border p-4 rounded shadow">
      <img [src]="product.image_urls[0]" alt="{{product.titre}}" class="w-full h-48 object-cover rounded mb-2">
      <h2 class="font-semibold">{{product.titre}}</h2>
      <p class="text-gray-700">{{product.description}}</p>
      <p class="mt-1">Prix: {{product.price}} MGA / {{product.unit}}</p>
      <p>Disponible: {{product.quantity_available}}</p>
      <button (click)="addToCart(product)"
              class="mt-2 bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded">
        Ajouter au panier
      </button>
    </div>
  </div>

  <!-- Panier -->
  <div *ngIf="cart.length > 0" class="mt-8 p-4 border rounded shadow bg-gray-50">
    <h2 class="text-xl font-bold mb-4">Votre panier</h2>

    <div *ngFor="let item of cart" class="flex justify-between items-center mb-2">
      <span>{{ item.product.titre }}</span>
      <div class="flex items-center space-x-2">
        <input type="number" [(ngModel)]="item.quantity" min="1"
               (ngModelChange)="updateQuantity(item, $event)"
               class="border rounded px-2 py-1 w-20 text-center">
        <span class="text-gray-600">{{ item.product.price * item.quantity }} MGA</span>
      </div>
       <button (click)="removeFromCart(item.product.id)"
            class="bg-red-500 hover:bg-red-600 text-white px-2 py-1 rounded">
      🗑️
    </button>
    </div>

    <!-- Total -->
    <div class="flex justify-between items-center mt-4 font-bold text-lg">
      <span>Total :</span>
      <span>{{ getCartTotal() }} MGA</span>
    </div>

    <button (click)="checkout()" class="mt-4 bg-green-500 hover:bg-green-600 text-white px-4 py-2 rounded w-full">
      Passer la commande
    </button>
  </div>

  <!-- Message -->
  <p *ngIf="message" class="mt-4 text-green-600 font-semibold">{{message}}</p>
</div>
